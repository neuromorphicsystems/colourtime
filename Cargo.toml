[package]
name = "colourtime"
version = "0.1.0"
edition = "2018"

[lib]
name = "colourtime"
crate-type = ["cdylib"]

[dependencies]
pyo3 = {version = "0.18.1", features = ["extension-module"]}
numpy = "0.18"
